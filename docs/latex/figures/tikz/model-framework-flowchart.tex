\begin{tikzpicture}[node distance=7mm and 12mm]
  \node[cse stage] (state) {Local phase state};
  \node[cse stage,below=of state] (properties) {Physical properties};
  \node[cse stage,below=of properties] (thermo) {Thermodynamic closure};
  \node[cse stage,below=of thermo] (transport) {Transport and hydraulics};
  \node[cse stage,below=of transport] (balances) {Packed-column balances};
  \node[cse solver,below=of balances] (solver) {BVP solver update};

  \node[cse detail,right=of state] (state-detail)
    {Flow, composition, pressure, and temperature};
  \node[cse detail,right=of properties] (properties-detail)
    {Thermophysical and transport properties};
  \node[cse detail,right=of thermo] (thermo-detail)
    {Coupled nine-species reactive EOS; reduced six-species chemistry only for the historical comparison};
  \node[cse detail,right=of transport] (transport-detail)
    {Fixed transport and enhancement formulas; thermodynamic inputs determine values};
  \node[cse detail,right=of balances] (balances-detail)
    {Material and energy balances over the packed height};
  \node[cse detail,right=of solver] (solver-detail)
    {Residual-controlled state update under the selected numerical method};

  \begin{scope}[on background layer]
    \node[cse region,fit=(state)(properties)(thermo)(transport)(balances)(solver)]
      (sequence-region) {};
    \node[cse region,fit=(state-detail)(properties-detail)(thermo-detail)
      (transport-detail)(balances-detail)(solver-detail)] (responsibility-region) {};
  \end{scope}

  \draw[cse arrow] (state) -- (properties);
  \draw[cse arrow] (properties) -- (thermo);
  \draw[cse arrow] (thermo) -- (transport);
  \draw[cse arrow] (transport) -- (balances);
  \draw[cse arrow] (balances) -- (solver);

  \draw[cse link] (state.east) -- (state-detail.west);
  \draw[cse link] (properties.east) -- (properties-detail.west);
  \draw[cse link] (thermo.east) -- (thermo-detail.west);
  \draw[cse link] (transport.east) -- (transport-detail.west);
  \draw[cse link] (balances.east) -- (balances-detail.west);
  \draw[cse link] (solver.east) -- (solver-detail.west);

  \coordinate (feedback-west) at ([xshift=-10mm]solver.west);
  \coordinate (feedback-top) at ([xshift=-10mm]state.west);
  \draw[cse arrow]
    (solver.west) -- (feedback-west) -- (feedback-top) -- (state.west);

  \path ([yshift=-4mm]current bounding box.south) node[inner sep=0] {};
\end{tikzpicture}
