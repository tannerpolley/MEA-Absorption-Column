# Carbon-capture motivation and story — 2026-09-03

The abstract and Introduction restore the emissions-reduction purpose of post-combustion capture, MEA's reference-process role, and the separation/energy/operating needs motivating absorber prediction. The opening now leads from climate and emissions to capture, MEA, coupled transfer/thermal behavior, the existing literature review, and the controlled model comparisons. Closing bridges return the abstract and Introduction to model selection and capture design. Retained prior manuscript framing and existing bibliography support the additions; no deployment statistics, new references or empirical findings were introduced. All 21 earlier Introduction citation keys remain among the current 23.

Independent story/source review found the argument coherent and the claims supported by the retained Harun, Freguia, Mores and Rodriguez source descriptions. The study scope, six Results subsections, numerical insertions and outstanding result obligations are unchanged. The generated map contains 153 body-section paragraphs and 667 prose source lines; all five source hashes match.

The canonical PDF remains 42 pages. Build and freshness checks pass; citations and references resolve, with only the existing CAS title-box warning. Seven checklist checks and scoped whitespace validation pass. Independent full-size visual inspection covers all 42 pages in color and grayscale: pages 1–21 and 22–42, with no material visual defect. The automatic report remains unchanged at builds/capture-motivation-qa: status fail, 217 signals, including six equation-glyph extraction errors on pages 5, 32, 34 and 37. These overlap signals are visually clean.

Live/offline checklist counts remain 17 inserted and 18 missing; reviewer scores are unchanged, with 18 completed and zero reassessment flags. No model run, result promotion, bibliography edit, commit, push or submission occurred.

---

# Scoped supporting results — 2026-09-03

The investigator-authorized isothermal Henry/eNRTL comparison now appears in Section 4.1 with its own retained vector profile figure and constructed feed/geometry/numerical specification in Methods. The 33-point initial-mesh capture values are 98.86120750035371% and 96.73351866554502%; their difference is −2.127688834808694 percentage points. The largest initial-mesh change is 1.0545846862441977e-8 percentage points. The copied figure is byte-identical to its producer PDF. The imposed-temperature control is explicitly separate from experimental error and the primary three-model comparison.

Section 4.4 reports the retained synthetic-node derivative-assembly check with its discrepancy definition and scoped interpretation. The supporting-results.zip supplement preserves the exact control inputs, results, plotted values, hash-matching calibrated eNRTL model/parameter files, and the synthetic verification record/test definition. The producer's evolving absorber adapter is identified by its original recorded hash, not silently replaced with current source. No model or test of the scientific runtime was executed. Independent numerical/source review confirmed the numbers and figure, then confirmed correction of the stale source note and addition of supporting input/test traceability. The main appendix and empirical result obligations remain open.

The canonical PDF builds to 42 pages, with no unresolved citations/references and only the existing 117.0831 pt CAS title-box warning. Full visual coverage reuses the preceding reviewed first 13 pages and independently inspects pages 14–27 and 28–41 in both color and grayscale. Final changes affect only the bodies of pages 26, 27, 40, 41 and new page 42; all ten final images were inspected again, including footer numbering. All other page bodies are pixel-identical to the prior reviewed render after excluding the footer page-count change. No material visual defect was found. Page 42 contains the last two bibliography entries. The final automatic report remains unchanged at builds/supporting-results-final-qa: status fail, 213 signals, including six superscript/extraction false positives on pages 5, 32, 33 and 37.

Seven checklist checks and PDF freshness pass. The two scoped completed rows raise the manuscript inventory to 35 rows: 17 inserted and 18 missing. Reviewer scores remain unchanged, with 18 completed rows and zero evidence-reassessment flags. Figure titles track compiled numbering. Offline pages were regenerated; the live server remains running. The generated story map contains 151 body-section paragraphs and 658 prose source lines, with all five source hashes verified. No commit, push or submission was performed.

---

# Literature continuity and comparison scope — 2026-09-03

The Introduction now separates rate-based/pilot evaluation, thermodynamic literature, and reactive-transfer/numerical literature before stating the paper's contribution. All 21 citation keys from the archived revised Introduction remain; the current Introduction has 23. The thermodynamic synthesis restores the specific roles of PC-SAFT association, SAFT-HR, electrolyte thermodynamics, and mixed-solvent work without treating different solvents as direct MEA validation. Multi-case NCCC capture and temperature evaluation is explicit in the Introduction and scope. The approved Results sequence remains unchanged.

The abstract, model, Methods, Results framing and conclusion now distinguish the nine-species ePC-SAFT model from the six-species eNRTL comparator. The six species and two combined reactions are printed explicitly in Equation 24 and linked from the thermodynamic input table. Both reactions preserve all four component/charge balances in the inspected six-species source record. Comparison wording distinguishes combined thermodynamic/reaction-model effects from an isolated closure substitution. Henry-reference wording no longer implies an interaction-parameter model. Both NCCC campaign sources are cited in Methods. No new coefficient set, numerical result or source publication was adopted.

An independent scientific prose review identified the explicit species/reaction scope, Henry-reference wording and campaign citations as corrections; follow-up confirmed all three resolved with no further bounded review defect. SOURCE_MAP.md records the retained source roles and comparator evidence. The generated story map indexes 146 body-section paragraphs and 635 prose source lines; all five source hashes match. Seven checklist checks, canonical build, PDF freshness and scoped whitespace validation pass. Live manuscript counts remain 15 inserted / 18 missing across 33 rows; reviewer counts remain 18 completed across 29 rows, with zero evidence-reassessment flags. Scores were not raised for editorial changes.

The canonical PDF is 41 pages. Independent visual inspection covers pages 1–20 and 21–41 in both color and grayscale. After the final paragraph-boundary correction, only page 7 differs from those inspected renders; the parent reinspected it in both modes and verified all other images byte-identical. No material clipping, overlap, illegible table or detached caption was found. The unchanged automatic report under builds/literature-scope-reviewed-qa retains status fail and 200 signals (194 warnings, six errors). Error signals on pages 5, 31, 32 and 36 are visually verified superscript/equation extraction false positives. The existing CAS title-box warning remains; no unresolved references or citations were introduced.

Offline checklists were regenerated and the live server remains running. No model campaign, result promotion, bibliography edit, commit, push or submission was performed.

---

# Section order and paragraph cohesion — 2026-09-03

The investigator-approved Results order is 4.1 thermodynamic comparison, 4.2 thermodynamic parameter sensitivity, 4.3 film transport and mobility response, 4.4 numerical methods, 4.5 LHC operating response, and 4.6 engineering interpretation. Procedures, perturbation definitions, ranges and the film sensitivity equation are in Methods; the accuracy/runtime result table is in Results. Existing paragraph IDs, equations, figure/table labels, literature review and technical appendix are preserved. New transitions and the conclusion follow the same sequence. Repeated comparison explanations were shortened; the Methods comparison paragraph was split by thermodynamics, film and numerics. The numerical design table now includes the already-described conservative simultaneous formulation.

The generated builds/manuscript-story-map.md indexes 143 body-section paragraphs and 614 prose source lines, with sentence IDs derived from stable paragraph IDs. Its five source hashes match the current manuscript. Source-wide paragraph-ID uniqueness, compiled Results numbering, figure-number/checklist mapping and whitespace checks pass. An independent prose reviewer found no lost equations or broken references and confirmed the requested narrative order. The review identified an LHC response-list mismatch, corrected by naming both phase outlet temperatures and temperature maxima/locations in the Results introduction. Study design stays beside the operating inputs, followed by the shared numerical implementation and accuracy procedures.

The canonical PDF remains 40 pages. XeLaTeX/BibTeX build and freshness checks pass, with no unresolved citations/references or new TeX warnings; the existing 117.0831 pt CAS title-box warning remains. Full color/grayscale visual inspection covers all 40 pages: one reviewer inspected 1–20, another 21–40, and the final changed page 26 was inspected again by the parent and reviewer. All other images were verified byte-identical to the inspected renders. No visible clipping, collisions, misplaced captions or illegible tables were found. Final automated output is retained unchanged in builds/story-reorder-reviewed-qa: status fail, 194 conservative findings. Error signals on pages 5, 30, 32 and 35 are visually verified equation/superscript geometry false positives.

Seven checklist checks pass. The live and offline checklists reflect the revised subsection and figure numbers; 33 manuscript rows remain 15 inserted and 18 missing. The existing grouped conclusion row additionally tracks the operating/engineering finding. All 29 reviewer rows retain their scores and exact original quotations; 18 are completed, including 14 original reviewer points, with zero evidence-reassessment flags. Rearrangement closes no empirical result obligations. The read-only checklist server was restored on its existing port and remains running. Cleanup audit reported pre-existing cache candidates; none were removed. No model run, result promotion, bibliography edit, commit, push or mirror synchronization was performed.

---

# Sensitivity methods and result-section preparation — 2026-09-03

The canonical manuscript builds to 40 pages. The new Methods 3.7 documents the agreed nine-calculation thermodynamic design, five-reaction direction coefficients and pivot-preserving perturbation, 48-point Latin hypercube with 15 inputs, dependent flow/composition construction, property propagation, and response-analysis method. The printed reaction vectors and all nine prescribed scenario shifts match the retained design within printed precision. Case 3C gas basis, assumed liquid inlet temperature, moisture/mass-flow convention, measured capture spread and temperature-coordinate conversion are explicit. Signed capture bias now has an equation. Abstract, Introduction and Conclusions include the local study without asserting its outcomes.

Results 4.4 now separates thermodynamic sensitivity and operating-condition interpretation; each has a figure layout and a distinct quantitative insertion. The prior integrated interpretation is 4.5. Two completed design/input rows and two required result-figure rows make the checklist 15 inserted / 18 missing / 33 total. Original reviewer completion remains 14/20; R1.8 and R2.5 move from 2 to 5 on the internal 10-point scale for their completed design documentation and remain In progress. Historical manuscript text already carried over is preserved.

The checklist parser now matches complete marker lines, preventing finding-1 from also matching finding-10. The new focused behavior check and six existing checks pass. The live server was restarted with the revised parser and the browser confirms the checked LHC input row and related reviewer links. Evidence hashes were reviewed for the changed prose; no prior completed reviewer item is reopened by these additions.

Independent scientific prose review confirmed the pivot identity for all five reactions and the source case/profile mapping. Follow-up clarifies kJ-to-J conversion, modeled saturation, the wet-gas mass basis and hourly-to-second conversion, assumed liquid inlet temperature in the range table, local versus full-column outputs, and both phase temperature maxima. The operating table now attributes 2014 rows to Morgan2018a and 2017 rows to Morgan2020. Neutral operating-comparison wording replaces an implied favorable-condition judgment. Present-tense method descriptions and framed result captions are retained as the investigator's requested complete manuscript structure; numerical findings remain explicit insertion blocks.

The canonical XeLaTeX/BibTeX build, freshness check, source whitespace check and unique-paragraph-ID check pass. No unresolved citation/reference or new TeX warning appears; the existing 117.0831pt CAS title-box warning remains. Full-PDF CSE visual QA is retained under builds/sensitivity-prose-reviewed-qa: 40 pages, 197 automated findings, status fail. Manual inspection of every page in color and grayscale found no visible clipping, text overlap, detached captions or unreadable tables. Parent inspected 1–20; independent visual reviewer inspected 21–40. Equation superscript/subscript overlap signals are extraction false positives; the automated report is retained unchanged. After the source clarifications, every changed render was inspected again; unchanged page images were verified byte-identical to the prior reviewed images.

This pass contains input/design documentation and result-independent interpretation only. It does not promote a numerical notebook, change a model runtime, fill unavailable outcomes, or assert final submission readiness. The cleanup audit reports pre-existing cache candidates, which are preserved; the user's live checklist remains running.

---

# Reviewer restoration and input documentation — 2026-09-03

The 36-page article restores the named literature review, model strengths and limitations, the explicit ePC-SAFT fugacity-benchmark definition, qualitative transport uncertainty, and applicability/other-solvent discussion from the superseded revision, adapted to the current formulation. Supported input additions distinguish fitted, assigned, and historically retained parameters; document fitting versus evaluation data; report solvation factors; and state the solvent-only dielectric equation. Independent text and parameter reviews informed the final corrections. No old numerical results were imported.

The live reviewer assessment closes 14 of 20 exact feedback points and 4 of 8 additional revision items. Six reviewer points remain open: thermodynamic sensitivity, numerical evidence, computational cost, reproducibility inputs, validation comparisons, and operating analysis. R2.3 retains the portable eNRTL records/mapping, final numerical settings, primary-source locators, and final software/data identities. Before scores and exact reviewer quotations are unchanged. All evidence hashes match; manuscript content remains 13 inserted / 16 missing across 29 rows.

Canonical build, PDF freshness, five checklist tests, live API assertions, and scoped whitespace checks pass. The final TeX log contains no unresolved references/citations and only the existing 117.0831 pt CAS title-box warning. Bibliography display now suppresses private exported note fields without modifying the Zotero bibliography; the Bülow reference and all 34 references render normally.

Full-size color/grayscale review covers all 36 pages, with independent inspection of pages 1–18 and parent inspection of pages 19–36. Final render hashes identify changes only on pages 26–32 and 36 after the last input-wording/bibliography corrections; each changed page was reinspected in both modes. No visible clipping, overlapping text, detached captions, or contrast defect was found. The retained automatic report at `builds/reviewer-restoration-qa/report.json` remains `fail`: 170 conservative warnings and six equation-overlap signals. The six signals on pages 5, 27, 28, and 32 are superscript/extraction-geometry false positives confirmed visually; the automatic status is not overwritten. Result and missing-input placeholders remain intentional author insertions.

---

# Matched reaction and caloric parameter update — 2026-09-03

The article now reports the retained ac57 `MEA_reactive_epcsaft_2026_09_03` selection: parameter SHA-256 `a9186c93759f2e2c02a6c913350ad06a244fff3f82503820c9962b3df8dd40d9` and caloric fingerprint `sha256:6919acbc3f1125b89363dd6997fcfd936036854768cd4c47d845d0133c8b0648`. Direct inspection confirms default selection and typed-reaction replacement in the absorber. The fitted R2 coefficient already includes the molality conversion; its separate offset is zero. R1/R3 remain unchanged. All nine reference enthalpies and 72 Cp coefficients were replaced with the matching reconstruction. Existing result files and their run identities were preserved.

The delivered PDF remains 34 pages. Its final TeX log is `builds/main.log`, with the canonical command output in `builds/parameter-update-build.log`; `builds/parameter-update-validation.json` records the PDF hash and exact identities. The adjacent `main.log` is a stale 31-page intermediate, not the delivered build. The final log has no unresolved citations or references and only the previously recorded CAS title-box overfull warning. Freshness and four checklist tests pass; the offline checklist was regenerated at `builds/manuscript-checklist.html`. Full-size color/grayscale inspection of the affected reaction/caloric tables and continuation (pages 24, 31, 32) found no clipping or overlap. Images are under `builds/parameter-update-qa/`.

The 1,001-point transcription check over 293.15–393.15 K reads all nine enthalpies and 72 coefficients back from LaTeX. Maximum rounding effects are 4.94e-7 J mol^-1 K^-1 in Cp and 2.88e-5 J mol^-1 in integrated h; exact metrics are in the validation record.

Independent numerical review confirms all five effective reaction equations and the nine/72 caloric entries within printed precision. Its conditional precedence concern is resolved by direct inspection of ac57 `reactive_bundle.py`: typed fitted records replace the complete reaction coefficient set and set `standard_state_offset` to zero. The retained source reaction-system file remains unchanged by design; consumers must honor that replacement. No new runtime test was inferred from this inspection.

This is a matched input-table update; no solver campaign, Engine installation, result relabeling, or column-result claim was made. The source map retains both new input identities and previous table/result identities.

---

# CSE setup and manuscript writing pass — 2026-09-03

The canonical CAS/XeLaTeX manuscript remains 34 pages. Current CSE routing now points to populated README and CONTEXT records, the existing manuscript/source map/checklist owners, and local writing rules. Historical finite-rate and fixed-chemistry records are explicitly scoped. No duplicate model documents, new analysis scaffold, Quarto manuscript, solver run, or result promotion was introduced.

The prose pass makes the abstract and comparison framing more direct and distinguishes conditional model substitutions from additive attribution. Prose source uses one sentence per line, blank-line paragraph boundaries, and 146 unique stable paragraph identifiers across eleven files. Equations, labels, tables, and figure commands retain their semantic structure. Source-only reflow, including the final abstract/author-ID correction, leaves extracted PDF text byte-identical; wording changes were completed before that comparison.

Routing checks, scoped prose checks, four existing checklist tests, manuscript whitespace checks, canonical build, and PDF freshness checks pass. The live checklist API returns 13 inserted / 16 missing / 29 total, no source problems, and a current PDF. The abstract and Sections 1–3 retain their complete value-independent prose. Incoming eNRTL reference states and the newer upstream reaction/caloric selection are recorded in SOURCE_MAP.md; the printed caloric table remains explicitly tied to its earlier coherent parameter snapshot until a final run selects its replacement.

The automatic 34-page article visual report is retained under `builds/cse-setup-visual-qa/`: status `fail`, 165 warnings and six equation-overlap signals. This automated status is not overwritten. Full-size color/grayscale inspection of pages 1–17 found no visible clipping or overlap, including the apparent-volume superscripts on page 4 and RMSE expression on page 16. The sole TeX overfull warning remains the pre-existing 117.0831 pt CAS title box. Independent full-size color/grayscale inspection covers pages 18–34 and found no visible clipping, detached captions, text collisions, or contrast defects. The page-26 and page-30 overlap signals are superscript extraction false positives. Together the two inspections cover all 34 pages in both modes.

Independent writing/setup review found no blockers. The last conclusion finding and final synthesis intentionally share paragraph `conclusion-08`; its checklist markers scope only the numerical insertion. The generated sentence report checks identifier uniqueness for this pass; the existing checklist tests remain focused on checklist behavior, rather than adding a new style-test suite. This acceptance covers writing, setup records, and layout only.

The existing CSE repository hook registration and stable runtime remain operational but precede the installed plugin's current hook assets. The setup skill treats shared runtime maintenance separately; this manuscript pass does not upgrade machine-wide hooks or claim that runtime is current. The repository cleanup audit reports pre-existing cache candidates; they are preserved, along with unrelated dirty work and the live checklist server.

---

# Living manuscript and current-science intake — 2026-09-03

The canonical raw-LaTeX manuscript now builds to **34 pages**. This pass adds the complete Engine mass/material/charge constraints and normalized feed derivative, distinguishes physical balances from numerical coordinates, documents the conservative CasADi/IPOPT formulation and its Jacobian/Hessian requirements, and sharpens the conditional three-study interpretation. Full-column film comparisons hold models and inputs fixed while solving each arm's axial states independently. The existing diagram now names the conservative formulation.

The caloric appendix now explains three neutral anchors, five transformed reaction enthalpy relations, and one ionic reference convention. It includes all nine reference enthalpies and 72 Cp coefficients from the selected v2 thermal record. At table generation, the selected parameter JSON was byte-identical to the absorber reactive bundle; the producer has since replaced that file, as recorded in the current SOURCE_MAP.md intake. The exact Engine identity and remaining final-runtime/source-domain reconciliation are recorded in SOURCE_MAP.md and the checklist review note; inserting these inputs does not assert a completed coupled-column result. No model campaign or historical result promotion was performed.

Checklist: **13 inserted, 16 missing, 29 total**; the main missing assets are eight figure/finding pairs and four input/result table groups. All thirteen figure/table rows carry a scientific question, quantities and units, cases/arms, evidence type, numerical requirements, owner/input locations, and permitted interpretation in Details. The simple full-width rows, square boxes, manuscript numbering, and live refresh remain intact. The server was reused until its session ended, then restarted on the same port after verifying no listener remained.

Validation: four focused checklist tests pass; the live API reports no source issues and a current PDF. Manuscript-scoped whitespace validation passes (unrelated existing analysis HTML whitespace was left untouched). The prescribed build resolves references and citations; the sole overfull warning is the pre-existing CAS title box (117.0831 pt). Visual inspection covers the changed introduction/invariants, diagram, conservative method and derivative equations, settings table, and caloric construction/coefficient tables. No visible clipping or overlap was found. PDF text contains no pending/worktree/CHECKLIST language. Browser inspection confirms the counts, expandable caloric provenance, and search over delivery details.

An independent numeric transcription check read all 72 printed coefficients back from LaTeX and compared the scaled polynomials over 293.15–393.15 K. Maximum rounding effects were 5.02e-7 J mol^-1 K^-1 in Cp and 2.85e-5 J mol^-1 in integrated h. This checks transcription only; it is not a fresh thermodynamic validation. Screenshots are under `builds/living-manuscript-qa/`.

---

# Content completion and figure/table checklist — 2026-09-03

The abstract and Sections 1–3 now have complete value-independent writing. The existing introduction and methods narrative are preserved; the abstract's contribution sentence is finished without asserting unknown outcomes. Added definitions cover excess-Gibbs activities, mole-fraction/molality reference conversion, gas-side transferred enthalpy, and the common gas caloric reference. A repeated excess-Gibbs derivative display was replaced with a cross-reference.

Five named table layouts now hold the absent numerical settings, accuracy/runtime results, sensitivity multipliers, additional thermodynamic records, and nine-species caloric inputs. Bracketed cells remain explicit placeholders. The original eight result panels and result-dependent interpretations remain to fill.

The checklist has 29 content rows: 12 complete and 17 open. The old 38-row list grouped eight figure/result-paragraph pairs and three conclusion findings, and added the already-written Introduction. Every linked finding remains machine-checked. Source attribution and final run reconciliation stay visible as review notes without blocking completed writing. Eight figures and five tables are the principal remaining insertions; the other four rows are cross-study interpretation, conclusions, and data/code identities.

The PDF builds successfully to 31 pages. All four checklist checks pass, including table presence, bracketed values, linked text, and nonblocking review notes. Browser inspection confirms the counts, square checkboxes, manuscript numbering, and Figures & tables filter. Visual inspection covers the abstract, new equations, and all five table layouts; no new overfull box or unresolved reference/citation was introduced. The existing CAS title-box warning remains. No model run or result promotion was performed.

Independent inspection confirmed that the five tables and linked figure/result paragraphs remain open. It also identified the already-recorded countercurrent runtime energy-sign mismatch. The physically consistent manuscript convention is retained; the existing author-only result-selection note in SOURCE_MAP.md and the energy row's review note require corrected runtime results. This pass does not import or certify affected numerical results.

---

# Targeted methods and appendix completion — 2026-09-03

The ten-item writing pass expands the manuscript from 25 to 29 pages. It preserves the prior introduction, numerical-method narrative, scientific comparison structure, and result panels. Applicable archived enhancement, transfer, and property equations were reused and checked against current implementation inputs. The current code supersedes older kinetic coefficients, volume expressions, and gas-viscosity forms.

The conserved-basis and common-model-input checklist blocks are populated. Eight other blocks have substantive text/tables and explicit nonprinting CHECKLIST-REMAINING instructions; the page labels them Drafted and keeps their boxes unchecked. The other 28 items retain their original status. The checklist's existing persistent check covers the new partial state, and all three checks pass.

Direct checks cover B times reaction stoichiometry, charge conservation for all five reactions, column area and hydraulic diameter, and every one of the 36 pair coefficients against the compact printed rule/table. The canonical bibliography is unchanged. No solver campaign or scientific result was generated or promoted by this writing pass.

The complete manuscript build and freshness check pass. Visual inspection covers the changed methods and appendix pages under `builds/methods-fill-qa/`; equations and tables remain within the text area. Long inline surface-coefficient lists were converted to aligned displays. The existing CAS title-box warning is recorded in the historical review below.

Independent review found no formula, value, or duplication errors in the ten populated blocks. Its wording clarification was applied: the enhancement reference uses one shared ionic diffusivity, while the equilibrium-manifold film uses species-resolved mobility values. The reviewer confirmed the two inserted/eight partial checklist states and that remaining-work comments do not print.

---

# Previous full manuscript and equation carryover — 2026-09-03

The current CAS manuscript is 25 pages. The full scientific rewrite supersedes the presentation-only checkpoint below. `SOURCE_MAP.md` records the eight result-figure replacements, nine Results passages, three conclusion passages, abstract additions, and 17 groups of exact method/parameter/source details still to insert. It is an author working document, not part of the rendered paper.

The visual-pilot comparison confirmed retention of the native TikZ diagram/styles, portrait table layouts, booktabs, siunitx, threeparttable, caption-owning floats, microtype, xurl, and CAS page furniture. The follow-up adds 21 ordinary and 14 partial derivative commands, an upright integration differential, and semantic linked references (25 `cref`, 11 `Cref`, one range). Equation operands, fixed-variable subscripts, units, values and labels are retained. No Quarto renderer, configuration, helper or second manuscript edition was imported.

Build: `TEXMFHOME=/home/tnnrpolley21/texmf bash docs/latex/scripts/build_main.sh`. The complete build and freshness check pass. The 15-file active input tree contains 63 unique labels and 38 resolved reference targets, with no duplicate labels or undefined citations/references. The one existing CAS title-box warning (117.08 pt) remains; no new overfull or underfull warning is present.

CSE visual QA output is under `builds/manuscript-carryover-qa/`. The article runner completed text/vector checks and rendered all 25 pages in color and grayscale at 110 dpi. Its automatic status is `fail`: 103 conservative clearance warnings and one text-overlap signal on page 20. Full-size inspection of every color and grayscale page found no visible clipping or overlap. The page-20 signal comes from the separate `app` superscripts in the apparent-volume equation, which are visibly positioned above their own symbols; it is an extraction-geometry false positive. Captions remain attached, the operating table is portrait, the flowchart is readable in grayscale, and the final bibliography page has normal trailing whitespace. The quantitative panels remain deliberate author placeholders.

The independent carryover inspection found no material equation tool missing. The pilot's unused state aliases, shaded-region style, and standalone drawing wrapper are not needed by the revised equations and diagram. Both bibliography files retain their pre-follow-up content, and all nonmanuscript files match the preserved snapshot. No model run, scientific-result promotion, branch integration, commit, push, or submission was performed.

## Historical presentation-only integration — 2026-09-03

The raw Elsevier CAS manuscript remains the sole article source. The comparison baseline is commit `4cd362c83e0a6b439655a18819a862da25e782cf`. The older visual pilot supplied table geometry and drawing styles; its manuscript prose, results, bibliography, and Quarto helpers were not imported.

## Content preservation

The eleven main, section, and appendix source files match the baseline after reversing the explicit quantity and derivative aliases and removing layout-only float commands. All numerical operating-condition, pure/binary-parameter, and solver-comparison rows match. Table captions and explanatory cells retain their original wording. Both bibliography files are byte-for-byte unchanged. An independent source comparison found no content drift.

Figure 1 is a document-native redraw of the existing raster. Its labels and directed relationships are retained, including the enhancement-factor labels that belong to this baseline. The later scientific revision must update those labels together with the governing formulation.

## Presentation changes

- `siunitx` quantities and numeric columns; five ordinary derivatives use `derivative` without changing their symbols.
- Six table layouts use booktabs rules, aligned numeric columns where applicable, and attached captions; the operating-condition notes use `threeparttable`.
- Figure 1 uses shared TikZ styles and explicit connector geometry.
- Real figure/table floats and `placeins` keep captions attached and Figure 1 beside the model introduction.

## Build and review

Build command: `TEXMFHOME=/home/tnnrpolley21/texmf bash docs/latex/scripts/build_main.sh`.
The explicit TeX root selects the already-installed Elsevier CAS files; no package was installed or copied into the manuscript.
The baseline has 33 pages; the integrated manuscript has 34 pages, including the full-page vector flowchart near its introduction.
The build and PDF freshness check pass, and references and citations resolve.
The baseline title-box warning of 117.08 pt remains; no new overfull box warning remains.

The CSE visual runner retains color/grayscale pages and geometry findings under `builds/presentation-release-qa/`.
Its conservative automated result reports 123 warnings and two equation text-overlap signals. Full-size color/grayscale review of pages 1–16 found the equation signals on pages 8 and 13 to be extraction-geometry false positives: accents, superscripts, and subscripts remain visibly separated. Figure 1 is contained and its branches are clear. Page 3 retains whitespace before the dedicated figure page.
Source comparison and visual acceptance apply only to this presentation checkpoint, not to later scientific edits.

Full-size color/grayscale review also covered pages 17–34. All redesigned tables fit without clipping, collisions, or detached notes. Remaining presentation limits are grayscale ambiguity in the unchanged solver bar chart and sparse continuation pages (20, 27, 29, 31, 34). These remain publication-polish items for the scientific revision, which will change pagination; the selected TikZ/table integration preserves content and is accepted with these limits.
