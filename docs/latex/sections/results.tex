    \section{Results}
        This section reports the MEA absorber benchmark results. The validation argument is not built around one successful simulation. It starts from the one-bed NCCC C cases, where both capture and temperature-profile data are available, and then separates the thermodynamic comparison so the effect of changing only the CO$_2$ fugacity driving force is clear.

        \subsection{Benchmark Model}
    
            A modular steady-state absorber model was used to simulate amine-based CO\textsubscript{2} absorption using pilot-plant cases from the National Carbon Capture Center (NCCC). Shooting, finite-difference, and collocation solution paths are evaluated against the same governing equations. The solver and thermodynamic comparison tables are regenerated from fixed input data and retain the run information needed to trace solver messages, residual norms, mesh settings, thermodynamic model identifiers, and case definitions.
            
            \begin{figure}
                \centering
                \includegraphics[width=12cm]{figures/temperature-lg.png}
                \caption{Qualitative liquid-to-gas-ratio sensitivity of the solved column temperature profile. The moving temperature bulge checks the coupled mass- and energy-balance response.}\label{fig:3D Temperature}
            \end{figure}
    
            Figure~\ref{fig:3D Temperature} shows the expected qualitative response when the liquid-to-gas ratio is varied. The temperature bulge moves through the packed height as the driving force and heat-release location change. This is a useful first check that the coupled mass and energy balances respond to operating conditions in the right direction. Quantitative validation then comes from regenerated benchmark results that report capture error, temperature RMSE, residual norm, and runtime.

        \subsection{NCCC Case Study}
        
            The model is benchmarked against the 2017 NCCC MEA test campaign for the one-bed C cases \cite{Morgan2020}. The campaign runs are listed in Table \ref{tab:mea-data}. Validation is reported with capture-rate error across the C cases and temperature-profile error for cases with measured temperature taps. Case 3C is kept as the representative profile comparison, but the full one-bed set carries the main validation argument.
            
            \input{tables/nccc_c_case_campaign}

        \begin{figure}
            \centering
            \includegraphics[width=0.9\textwidth]{figures/case-3c-temperature-validation.png}
            \caption{Representative Case 3C temperature-profile validation against experimental pilot-plant taps. The corrected campaign input has $\frac{L}{G}=4.72$, $\alpha = 0.25$, and inlet $y_{\mathrm{CO_2}} = 0.093$; both thermodynamic calculations reproduce the measured capture value within approximately one percentage point.}
            \label{fig:temp_profiles}
        \end{figure}
        Case 3C is used as the illustrative profile because it has measured temperature taps and a capture value close to the model prediction. The temperature data for this row are shown in Table~\ref{tab:3C-temp-profile}. It is not the only validation evidence; the verified one-bed C dataset covers all seven C cases.
        \input{tables/case_3c_temperature_profile}
        The solved temperature profiles are useful because they test the coupled energy balance, heat transfer, and absorption behavior. In Figure~\ref{fig:temp_profiles}, the Henry-law calculation predicts $\SI{88.33}{\percent}$ capture and the ePC-SAFT calculation predicts $\SI{88.75}{\percent}$ capture against the $\SI{89.50}{\percent}$ pilot-plant value. The Case 3C temperature-profile RMSE values are also close: \SI{4.18}{K} for Henry-law closure and \SI{4.21}{K} for ePC-SAFT. This figure is a clear example of the model behavior, while the full C-case set provides the validation statistics.

        \begin{figure}
            \centering
            \includegraphics[width=0.92\textwidth]{figures/c-case-thermo-benchmark.pdf}
            \caption{Benchmark comparison for the corrected one-bed NCCC C cases. Both thermodynamic calculations converge for all seven cases; the left panel reports case-by-case capture error and the right panel reports median runtime.}
            \label{fig:c-case-thermo-benchmark}
        \end{figure}

        Figure~\ref{fig:c-case-thermo-benchmark} extends the validation from Case 3C to all seven one-bed C cases in the corrected campaign-input dataset. The Henry-law and ePC-SAFT calculations both converged for every case and kept absolute capture error below ten percentage points. The ePC-SAFT calculation gives a modestly lower mean absolute capture error: 4.54 percentage points compared with 4.70 percentage points for Henry-law closure. ePC-SAFT took longer because vapor- and liquid-side fugacity coefficients are computed from the equation of state, and because the liquid side evaluates the ionic fitted-Born SSM+DS contribution terms. Even so, the median runtime was still \SI{18.93}{s} compared with \SI{13.93}{s} for Henry-law closure. That makes ePC-SAFT a practical middle-ground option when Henry's Law is too idealized but a full activity-coefficient recalibration would add more complexity than the benchmark needs.

        \begin{figure}
            \centering
            \includegraphics[width=0.90\textwidth]{figures/case-c-temperature-overlay.png}
            \caption{Temperature-profile overlays for all seven one-bed NCCC C cases with corrected campaign inputs. Markers are measured liquid-temperature taps; model curves show the Henry-law and ePC-SAFT thermodynamic calculations. The two thermodynamic closures give similar temperature-profile errors, while the ePC-SAFT run provides the electrolyte EOS fugacity calculation used in the capture comparison.}
            \label{fig:generated-temperature-profiles}
        \end{figure}

        The temperature profiles in Figure~\ref{fig:generated-temperature-profiles} support the outlet-capture results because they test the coupled mass-transfer and energy-balance response along the bed. Across the campaign cases, the mean temperature RMSE is \SI{6.88}{K} for Henry-law closure and \SI{6.91}{K} for ePC-SAFT. That difference is small enough that the temperature evidence should be read as comparable profile behavior rather than a separate thermodynamic win. Case 7C remains the hardest thermal-shape case, but the corrected inputs remove the earlier severe capture-trend pattern and leave a clearer target for future model development.

        \input{sections/results_benchmark_evidence}

       \begin{figure}
            \centering
            \begin{subfigure}[t]{0.48\textwidth}
                \centering
                \includegraphics[width=\textwidth]{figures/method-shooting.png}
                \caption{Shooting method}
                \label{fig:shooting}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.48\textwidth}
                \centering
                \includegraphics[width=\textwidth]{figures/method-collocation.png}
                \caption{Collocation method}
                \label{fig:collocation}
            \end{subfigure}
            \hfill
            \begin{subfigure}[t]{0.48\textwidth}
                \centering
                \includegraphics[width=\textwidth]{figures/method-finite-difference.png}
                \caption{Finite difference method}
                \label{fig:finite_difference}
            \end{subfigure}
            \caption{Temperature-profile examples from the shooting, collocation, and finite-difference solution paths. The panels illustrate the common profile shape used for method comparison before the quantitative solver benchmark is applied.}
            \label{fig:method_comparison_figures}
        \end{figure}

    \subsection{Simulation Method Comparison}
        Figure~\ref{fig:method_comparison_figures} shows temperature profiles from shooting, collocation, and finite difference. The profiles are similar enough to show that the same governing equations are being solved, but the methods still differ in speed, conditioning, and reliability. At higher $L/G$ ratios, the temperature bulge shifts toward the bottom of the column and shooting can be an efficient comparison method. Near thermodynamic pinch conditions, collocation is more reliable because it solves the BVP directly with mesh and residual controls.

        Collocation provides the adaptive boundary-value calculation used as the reference method for the NCCC validation cases. Its performance still depends on the initialization profile, scaling, mesh limits, and tolerance settings, so those values are reported directly.

        Finite difference provides a second comparison method by solving the spatially discretized equations as one nonlinear algebraic system. Its usefulness is judged with the same metrics as the other methods: convergence success, boundary residuals, validation error, mesh size, and runtime.

        Each method has a role in the benchmark. Shooting is the simplest and fastest comparison method on favorable cases. Collocation is the reference calculation for the measured NCCC temperature-profile cases. Finite difference provides a clear algebraic discretization for comparison. Table~\ref{tab:method_comparison_table} should be read alongside the quantitative benchmark outputs.
        \input{tables/numerical_method_comparison}

        The benchmark replaces qualitative method rankings with reported statistics. The comparison reports convergence behavior, boundary residuals, validation errors, and runtime across representative cases. On the current evidence, collocation is the reference method for the NCCC one-bed validation because it gives the most consistent capture and temperature-profile results for the thermal-pinch case. Shooting and finite difference remain useful on favorable SRP-style one-bed cases because they expose speed, stiffness, initialization, and nonlinear-system conditioning tradeoffs.


    \subsection{Limitations and Future Work}
        These results are a reproducible benchmark of a simplified absorber model, not a complete predictive process simulator. The ePC-SAFT calculations are limited to the CO$_2$ fugacity-coefficient driving force. They do not replace the chemical-equilibrium model, reaction-enhancement calculation, or transport-property correlations with a fully coupled electrolyte-reactive column residual. Activity-coupled speciation tests showed that ePC-SAFT activities can be routed into the chemistry calculation, but the present full-column activity-basis run was too slow for the repeated benchmark role of this paper. The next step is therefore not just to turn that option on; it is to make the activity-coupled speciation fast enough through caching, tabulation, continuation, or a re-fit of the activity-basis constants. The one-bed NCCC C cases provide the primary validation set with measured temperature taps, and the benchmark is most useful as a documented basis for solver comparison and thermodynamic sensitivity. Future absorption-column studies that need a balance between accuracy and speed can use this style of ePC-SAFT fugacity calculation as an intermediate thermodynamic choice. Fully audited electrolyte-reactive or eNRTL \cite{Zhang2011} comparisons should be reserved for work where the additional calibration and runtime cost are justified. Optimization and scale-up studies should remain future applications unless they are supported by validation data.


