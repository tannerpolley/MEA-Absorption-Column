## Change

-

## Issue

- `Closes #N` only if this PR completes the issue's acceptance; otherwise `Refs #N` (or `None`).
- After merge: update the issue body's current state and remaining criteria (replace, don't append), and remove `work:review`/`work:claimed` labels. A comment alone is not a handoff.

## Evidence

- Checks performed:
- Engine wheel SHA-256 (if Engine-dependent):
- Limits or deferred work:
